// GLOBAL SETTINGS
const API_BASE = "http://localhost:5001/api"; 
let allServices = []; // Shared service list

// UI Helper
function closeModal(id) { document.getElementById(id).style.display = 'none'; }